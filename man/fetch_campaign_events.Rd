% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_dataset_comparison.R
\name{fetch_campaign_events}
\alias{fetch_campaign_events}
\title{Fetch one row per campaign/episode onset from several conflict datasets}
\usage{
fetch_campaign_events(datasets, start_year, end_year, coding_system)
}
\arguments{
\item{datasets}{Character vector of \code{conflict_data()} dataset keys.}

\item{start_year, end_year}{Integer year bounds.}

\item{coding_system}{\code{"cow"} or \code{"gw"}.}
}
\value{
A data frame with \code{dataset}, \code{unit} (the coding-system code),
\code{year}, and \code{region} (World Bank 7-region classification, via
\code{countrycode::countrycode()}).
}
\description{
Fetch one row per campaign/episode onset from several conflict datasets
}
\details{
\code{"navco1.3"}, \code{"beissinger"}, \code{"csra"}, and \code{"mec"} are already one row
per campaign/episode. \code{"navco2.1"} is not: \code{conflict_data()} returns one
row per campaign-\emph{year}, so a single long-running campaign contributes
many rows. Comparing that directly against onset-coded datasets would
overstate NAVCO 2.1's event counts by roughly the average campaign
duration. To keep the comparison on the same onset-level footing as the
source article, \code{"navco2.1"} is filtered here to \code{nvc2.1_ONSET == 1},
\code{conflict_data()}'s own per-campaign onset flag (keyed on the numeric
campaign \code{id}, not the free-text campaign name, which is not unique --
e.g. three distinct "Myanmar Regime Change Campaign" entries share that
name). That flag is computed over the dataset's full native coverage
before \code{start_year}/\code{end_year} filtering, so campaigns already under way
at \code{start_year} are not mistaken for new onsets.

\code{countrycode::countrycode()}'s \code{"region"} destination is not a clean
7-category classification: a handful of pre-1990 historical entities
(Yemen Arab Republic, Yemen People's Republic, the United Arab
Republic) still carry the World Bank's older "Middle East & North
Africa" label, while every other country in that region carries the
newer, longer "Middle East, North Africa, Afghanistan & Pakistan"
label -- these historical COW/GW codes do appear in campaign data
covering the 1950s-80s, so left as-is they'd split one region into two
bars. The newer label is recoded to the shorter, older one here (kept
as the canonical form for display) so the region breakdown stays at
exactly 7 categories.
}
\keyword{internal}
